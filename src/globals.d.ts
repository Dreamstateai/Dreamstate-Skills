// gradient-string v2 ships without type declarations; we only use the default
// export as a callable that returns an object with .multiline(). Declare it
// loosely so strict TS is happy without pulling in a types package.
declare module 'gradient-string';
